-- Part 1: Test it with SQL
Create table jobs (
id INT,
employer VARCHAR(500),
name VARCHAR(500),
skills VARCHAR(60)
);


-- Part 2: Test it with SQL
SELECT * name
FROM job
WHERE job.name = St. Louis City

-- Part 3: Test it with SQL

-- Part 4: Test it with SQL